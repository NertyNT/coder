name: CI

on:
  push:
  pull_request:

jobs:
  lint-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check markdown formatting (basic)
        run: |
          echo "CI scaffold is ready. Add linters/tests in next iteration."
